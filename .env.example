# Root — backend / Supabase (server-side only)
SUPABASE_URL=https://your-staging-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# NEVER expose in client apps or Netlify. Rotate if ever committed.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: comma-separated CORS origins for edge functions
# ALLOWED_ORIGINS=https://thunderous-liger-d1d41c.netlify.app,http://localhost:8081
